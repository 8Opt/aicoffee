"""
`layers` defines each layers that are implemented to build a model.
`strategy` defines several appoach to train a model:
    + loss function.
    + optimization function.
    + early stopping.
    + dropping.
    + ...
`thirdparty` defines models that are:
    1. call via Huggingface.
    2. call via API (Gemini, OpenAI, Perplexity, etc. )
`dataset` defines supported datasets for:
    + computer vision.
    + text.
    + audio.    # ? should we
"""
